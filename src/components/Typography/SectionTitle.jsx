const SectionTitle = ({ children }) => {
  return (
    <h2 className="inline-block text-lg font-semibold mb-5 text-gray-700 dark:text-gray-200">
      {children}
    </h2>
  );
};

export default SectionTitle;
